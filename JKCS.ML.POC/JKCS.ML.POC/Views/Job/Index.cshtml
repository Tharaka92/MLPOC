@model IEnumerable<JKCS.ML.POC.Models.JobModel>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @*@Html.ActionLink("Create New", "Create")*@
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.JobId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Date)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.JobId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Date)
        </td>
        <td>
            @Html.ActionLink("Edit", null, new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Details", null, new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Delete", null, new { /* id=item.PrimaryKey */ })
        </td>
    </tr>
}

</table>
